# S3 버킷 생성 완벽 가이드 (초보자용)

이 가이드는 AWS 계정만 있다면 누구나 따라할 수 있도록 작성되었습니다.

---

## 📋 사전 준비

- [ ] AWS 계정 (없으면 https://aws.amazon.com 에서 가입)
- [ ] 신용카드 (무료 티어 사용 가능, 요금 걱정 말고 따라하세요)

---

## 1단계: AWS Management Console 접속

### 1-1. AWS Console 로그인

1. 브라우저에서 https://console.aws.amazon.com 접속
2. 이메일 주소 입력 → "다음"
3. 비밀번호 입력 → "로그인"

### 1-2. S3 서비스로 이동

**방법 1**: 검색 사용 (추천)
- 상단 검색창에 "S3" 입력
- "S3" 클릭 (Simple Storage Service)

**방법 2**: 메뉴 사용
- 좌측 상단 "서비스" 클릭
- "스토리지" → "S3" 클릭

### 1-3. 리전 확인

- 우측 상단에서 리전이 **"서울 (ap-northeast-2)"** 인지 확인
- 다른 리전이면 클릭해서 "아시아 태평양(서울)" 선택

---

## 2단계: S3 버킷 생성

### 2-1. 버킷 만들기 시작

1. 주황색 **"버킷 만들기"** 버튼 클릭
2. 버킷 생성 페이지가 열립니다

### 2-2. 일반 구성

**버킷 이름** (매우 중요!)
```
damoa-uploads-YOUR_NAME
```
예시: `damoa-uploads-john`, `damoa-uploads-kim123`

⚠️ 주의사항:
- 전 세계에서 유일해야 함 (이미 누가 쓰고 있으면 안 됨)
- 소문자만 가능
- 숫자와 하이픈(-) 가능
- 3~63자
- 띄어쓰기, 대문자, 특수문자 불가

**AWS 리전**
- "아시아 태평양(서울) ap-northeast-2" 선택됨 (이미 선택되어 있음)

### 2-3. 객체 소유권

**선택**: "ACL 비활성화됨(권장)" (기본값 유지)

설명: 최신 방식이며, 더 안전합니다.

### 2-4. 이 버킷의 퍼블릭 액세스 차단 설정 (⚠️ 중요!)

기본값은 모든 체크박스가 체크되어 있습니다.

**설정 방법**:
1. **"모든 퍼블릭 액세스 차단"** 체크박스를 **해제**
   - 또는 아래 4개 체크박스를 모두 해제:
     - [ ] 새 ACL을 통해 부여된 버킷 및 객체에 대한 퍼블릭 액세스 차단
     - [ ] 임의의 ACL을 통해 부여된 버킷 및 객체에 대한 퍼블릭 액세스 차단
     - [ ] 새 퍼블릭 버킷 또는 액세스 포인트 정책을 통해 부여된 버킷 및 객체에 대한 퍼블릭 액세스 차단
     - [ ] 임의의 퍼블릭 버킷 또는 액세스 포인트 정책을 통한 버킷 및 객체에 대한 퍼블릭 및 크로스 계정 액세스 차단

2. 경고 메시지가 나옵니다:
   ```
   ⚠️ 퍼블릭 액세스를 허용하면 인터넷의 누구나 이 버킷에 액세스할 수 있습니다.
   ```

3. **"현재 설정으로 인해 이 버킷과 그 안에 포함된 객체가 퍼블릭 상태가 될 수 있음을 알고 있습니다"** 체크박스 체크

**왜 이렇게 하나요?**
- 사용자들이 업로드한 이미지를 웹에서 볼 수 있어야 하기 때문입니다.
- 나중에 버킷 정책으로 읽기만 허용하고, 쓰기는 Presigned URL로만 가능하게 합니다.

### 2-5. 버킷 버전 관리

**선택**: "비활성화" (기본값 유지)

설명: 파일을 덮어쓸 때 이전 버전을 보관하는 기능입니다. 지금은 불필요합니다.

### 2-6. 태그 - 선택 사항

비워두거나 원하면 추가:
- 키: `Environment`, 값: `Development`
- 키: `Project`, 값: `Damoa`

### 2-7. 기본 암호화

**선택**: "서버 측 암호화 활성화" (체크됨 유지)

**암호화 유형**:
- "Amazon S3 관리형 키(SSE-S3)" 선택 (기본값)

설명: 파일이 암호화되어 저장됩니다. 무료이고 자동입니다.

### 2-8. 고급 설정

**객체 잠금**: 비활성화 (기본값 유지)

### 2-9. 버킷 만들기

- 하단 주황색 **"버킷 만들기"** 버튼 클릭
- 성공 메시지: "버킷이 생성되었습니다" ✅

---

## 3단계: CORS 정책 설정 (⚠️ 필수!)

### 3-1. 버킷 선택

- 방금 만든 버킷 이름 클릭

### 3-2. 권한 탭 이동

- 상단 탭에서 **"권한"** 클릭

### 3-3. CORS 편집

1. 아래로 스크롤하여 **"CORS(Cross-Origin Resource Sharing)"** 섹션 찾기
2. 우측 **"편집"** 버튼 클릭
3. 텍스트 박스가 비어있습니다
4. 아래 JSON을 **복사해서 붙여넣기**:

```json
[
  {
    "AllowedHeaders": [
      "*"
    ],
    "AllowedMethods": [
      "GET",
      "PUT",
      "POST",
      "DELETE",
      "HEAD"
    ],
    "AllowedOrigins": [
      "http://localhost:8080",
      "http://127.0.0.1:8080"
    ],
    "ExposeHeaders": [
      "ETag",
      "x-amz-request-id"
    ],
    "MaxAgeSeconds": 3600
  }
]
```

5. **"변경 사항 저장"** 클릭

**주의**: 나중에 실제 도메인을 사용한다면 `AllowedOrigins`에 추가하세요:
```json
"AllowedOrigins": [
  "http://localhost:8080",
  "http://127.0.0.1:8080",
  "https://yourdomain.com"
]
```

---

## 4단계: 버킷 정책 설정

### 4-1. 버킷 정책 편집

1. 같은 "권한" 탭에서 위로 스크롤
2. **"버킷 정책"** 섹션 찾기
3. 우측 **"편집"** 버튼 클릭

### 4-2. 정책 입력

1. 아래 JSON을 복사하되, **YOUR-BUCKET-NAME 부분을 실제 버킷 이름으로 변경**:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "PublicReadGetObject",
      "Effect": "Allow",
      "Principal": "*",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::YOUR-BUCKET-NAME/*"
    }
  ]
}
```

예시 (버킷 이름이 `damoa-uploads-john`인 경우):
```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "PublicReadGetObject",
      "Effect": "Allow",
      "Principal": "*",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::damoa-uploads-john/*"
    }
  ]
}
```

2. **"변경 사항 저장"** 클릭
3. 경고: "이 버킷에 퍼블릭 액세스 권한이 있습니다" → 무시하고 저장

**이 정책의 의미**:
- 인터넷의 모든 사람(`Principal: "*"`)이
- 버킷의 파일을 읽을 수 있습니다(`Action: "s3:GetObject"`)
- 하지만 쓰기, 삭제는 불가능합니다 (허용하지 않음)

---

## 5단계: IAM 사용자 생성 및 액세스 키 발급

백엔드 애플리케이션이 S3에 접근하려면 액세스 키가 필요합니다.

### 5-1. IAM 서비스로 이동

1. 좌측 상단 "서비스" → "보안, 자격 증명 및 규정 준수" → **"IAM"** 클릭
2. 또는 검색창에 "IAM" 입력 후 클릭

### 5-2. 사용자 생성

1. 좌측 메뉴에서 **"사용자"** 클릭
2. **"사용자 생성"** 버튼 클릭 (주황색)

### 5-3. 사용자 세부 정보 지정

**1단계: 사용자 세부 정보 지정**

- **사용자 이름**: `damoa-s3-uploader`
- **AWS Management Console에 대한 사용자 액세스 권한 제공**: 체크 **해제** (프로그램 전용)
- **"다음"** 클릭

**2단계: 권한 설정**

방법 선택: **"직접 정책 연결"**

1. 검색창에 `S3` 입력
2. `AmazonS3FullAccess` 체크 (또는 아래 사용자 지정 정책 사용)
   - ⚠️ 프로덕션에서는 최소 권한 원칙 적용하세요 (아래 참고)
3. **"다음"** 클릭

**3단계: 검토 및 생성**

- 설정 확인
- **"사용자 생성"** 클릭
- 성공 메시지: "사용자 damoa-s3-uploader를 생성했습니다" ✅

### 5-4. 액세스 키 생성 (⚠️ 중요!)

1. 방금 만든 사용자 이름(`damoa-s3-uploader`) 클릭
2. **"보안 자격 증명"** 탭 클릭
3. 아래로 스크롤하여 **"액세스 키"** 섹션 찾기
4. **"액세스 키 만들기"** 버튼 클릭

**액세스 키 생성 단계**:

1. **사용 사례**: "로컬 코드" 선택
2. 체크박스 체크: "위의 권장 사항을 이해했으며 액세스 키 생성을 계속하려고 합니다"
3. **"다음"** 클릭
4. **태그 설정 (선택)**: 건너뛰기 → **"액세스 키 만들기"** 클릭

**액세스 키 저장 (⚠️ 매우 중요!)**

화면에 다음 정보가 표시됩니다:

```
액세스 키: AKIAIOSFODNN7EXAMPLE
보안 액세스 키: wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
```

🔴 **중요**: 보안 액세스 키는 이 페이지를 닫으면 **다시 볼 수 없습니다!**

**저장 방법**:
1. **.csv 파일 다운로드** 버튼 클릭 (안전한 곳에 보관)
2. 또는 메모장에 복사해서 저장
3. **"완료"** 클릭

---

## 6단계: 환경 변수 설정

### 6-1. .env 파일 열기

프로젝트 루트 디렉토리의 `.env` 파일을 엽니다.

### 6-2. 다음 값들을 추가/수정

```properties
# AWS 자격 증명 (5-4에서 받은 값)
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# S3 버킷 설정 (2-2에서 만든 버킷 이름)
AWS_S3_REGION=ap-northeast-2
AWS_S3_BUCKET=damoa-uploads-john

# 파일 업로드 기본 URL (버킷 이름을 실제 이름으로 변경)
FILE_UPLOAD_BASE_URL=https://damoa-uploads-john.s3.ap-northeast-2.amazonaws.com
```

**예시** (버킷 이름이 `damoa-uploads-john`인 경우):
```properties
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
AWS_S3_REGION=ap-northeast-2
AWS_S3_BUCKET=damoa-uploads-john
FILE_UPLOAD_BASE_URL=https://damoa-uploads-john.s3.ap-northeast-2.amazonaws.com
```

### 6-3. 파일 저장

- Ctrl+S (Windows) 또는 Cmd+S (Mac)
- ⚠️ `.env` 파일이 `.gitignore`에 포함되어 있는지 확인 (GitHub에 올리면 안 됨!)

---

## 7단계: 애플리케이션 재시작 및 테스트

### 7-1. 애플리케이션 재시작

실행 중인 앱 중지 후 재시작:

```bash
# Windows
./gradlew.bat bootRun

# Mac/Linux
./gradlew bootRun
```

### 7-2. 테스트 페이지 접속

브라우저에서:
```
http://localhost:8080/image-upload-test.html
```

### 7-3. 업로드 테스트

1. **토큰 입력**
   - 로그인 후 받은 JWT 토큰 입력

2. **이미지 선택**
   - 아무 이미지 파일 선택 (JPG, PNG 등)

3. **업로드**
   - "업로드" 버튼 클릭
   - 진행률 확인:
     - 1/3: Presigned URL 요청 중...
     - 2/3: S3로 파일 업로드 중...
     - 3/3: 업로드 완료 처리 중...

4. **성공!**
   - "파일이 성공적으로 업로드되었습니다!" 메시지
   - 업로드된 이미지 목록에 표시됨

### 7-4. S3 버킷에서 확인

1. AWS Console → S3 → 버킷 이름 클릭
2. `uploads/2024/...` 폴더 안에 파일이 있는지 확인
3. 파일 클릭 → "객체 URL" 복사 → 브라우저에 붙여넣기
4. 이미지가 보이면 성공! ✅

---

## ✅ 완료 체크리스트

설정을 완료했는지 확인하세요:

- [ ] AWS 계정 로그인
- [ ] S3 버킷 생성 (버킷 이름: `damoa-uploads-xxx`)
- [ ] 퍼블릭 액세스 차단 해제
- [ ] CORS 정책 설정 (`PUT` 메서드 포함)
- [ ] 버킷 정책 설정 (Public Read)
- [ ] IAM 사용자 생성 (`damoa-s3-uploader`)
- [ ] 액세스 키 발급 및 안전하게 저장
- [ ] `.env` 파일에 AWS 자격 증명 및 버킷 정보 입력
- [ ] 애플리케이션 재시작
- [ ] 테스트 페이지에서 이미지 업로드 테스트
- [ ] S3 버킷에서 파일 확인

---

## 🔧 문제 해결

### "버킷 이름이 이미 존재합니다"
- 다른 이름 사용 (예: 뒤에 숫자 추가)
- 전 세계에서 유일해야 합니다

### "CORS 오류" (브라우저 콘솔)
```
Access to fetch has been blocked by CORS policy
```
→ 3단계 CORS 설정 재확인 (`PUT` 메서드 포함되었는지)

### "403 Forbidden" (업로드 시)
- 4단계 버킷 정책 재확인 (버킷 이름이 정확한지)
- 5단계 IAM 권한 재확인

### "401 Unauthorized"
- JWT 토큰 확인
- 토큰이 만료되었으면 재로그인

### "파일이 S3에 업로드되지 않음"
- 6단계 `.env` 파일 재확인
- 액세스 키와 시크릿 키가 정확한지
- 버킷 이름이 정확한지

---

## 💰 요금 안내

**무료 티어** (AWS 계정 생성 후 12개월):
- S3 저장: 5GB
- GET 요청: 20,000건/월
- PUT 요청: 2,000건/월

**무료 티어 이후**:
- 저장: $0.025/GB (약 30원/GB)
- GET 요청: $0.0004/1,000건
- PUT 요청: $0.005/1,000건

**예상 비용** (월 100명 사용 가정):
- 이미지 100개 (각 1MB): 0.1GB → $0.0025 (약 3원)
- 거의 무료입니다! 😊

---

## 📞 추가 도움

문제가 해결되지 않으면:
1. AWS 공식 문서: https://docs.aws.amazon.com/s3/
2. AWS 지원 센터: AWS Console → 우측 상단 "지원" 메뉴

---

**축하합니다! S3 설정이 완료되었습니다!** 🎉

이제 HTML 페이지에서 이미지를 업로드하면 S3에 저장됩니다!
